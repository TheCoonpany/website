---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<div class={className}>
  <slot />
</div>

<style lang="scss">
  div {
    width: clamp(320px, 100%, 1200px);
    margin-inline: auto;
    padding-inline: 2rem;
    display: grid;
  }
</style>
