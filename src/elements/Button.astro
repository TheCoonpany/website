---
interface Props {
  text?: string;
}

const $props = Astro.props;
---

<button data-el="button">
  <span>
    <slot>{$props.text}</slot>
  </span>
</button>

<style lang="scss">
  button[data-el="button"] {
    --_color: var(--x-button--color, #21ebff);
    position: relative;
    cursor: pointer;
    width: max-content;

    color: var(--_color);
    padding: 0;
    border: none;
    background-color: transparent;
    transition: all 0.3s ease;

    &,
    > span {
      &:before,
      &:after {
        position: absolute;
        content: "";
        background: var(--_color);
        box-shadow: 0 0 10px var(--_color);
        transition: all 0.3s ease;
      }
    }

    &:focus {
      --_color: #f88c07;
      outline: none;
    }

    &:focus,
    &:hover {
      &,
      > span {
        &:before {
          height: 100%;
        }
        &:after {
          width: 100%;
        }
      }
    }

    > span {
      position: relative;
      display: block;
      padding: 0.5rem 1rem;
      width: 100%;
      height: 100%;

      &:before,
      &:after {
        left: 0;
        top: 0;
      }
      &:before {
        width: 2px;
        height: 50%;
      }
      &:after {
        height: 2px;
        width: 20%;
      }
    }

    &:before,
    &:after {
      right: 0;
      bottom: 0;
    }
    &:before {
      height: 50%;
      width: 2px;
    }
    &:after {
      width: 20%;
      height: 2px;
    }
  }
</style>
