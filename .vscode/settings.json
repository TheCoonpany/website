{
  "liveServer.settings.root": "/dist",
  "liveServer.settings.port": 5500,
  "explorer.fileNesting.enabled": true,
  "explorer.fileNesting.patterns": {
    "*.astro": "${capture}.ts, ${capture}.scss, ${capture}.*.astro",
  }
}