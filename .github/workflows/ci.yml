name: CI
on:
  push:
    branches:
      - main

jobs:
  build:
    name: CI
    runs-on: ubuntu-slim

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        run: npm i -g pnpm

      - name: Install dependencies
        run: pnpm i --frozen-lockfile

      # Builds

      # - name: Build docs
      #   run: pnpm docs:build